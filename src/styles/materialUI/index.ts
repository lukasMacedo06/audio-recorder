import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#03363D',
    },
    secondary: {
      main: '#1890FF',
    },
  },
});

export default theme;
